class Solution {
public:
    int differenceOfSums(int n, int m) {
        int k=0;
        int p=0;
        for(int i=1;i<=n;i++)
        {
            if(i%m!=0) k+= i;
            else p+=i;
        }
        return (k-p);
    }
};
