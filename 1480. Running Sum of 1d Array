class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        vector<int> result;
        int k =0;
        for(int i=0;i<nums.size();i++)
        {
            k += nums[i];
            result.push_back(k);
        }
        return result;
    }
};
